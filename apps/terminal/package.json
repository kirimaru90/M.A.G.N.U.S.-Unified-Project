{
  "name": "magnus-terminal-emulator",
  "version": "0.0.0",
  "private": true,
  "description": "Dev/test tooling for the RobCo terminal emulator. The app itself ships as static files with no build step; this package exists only to run the Playwright end-to-end suite.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
